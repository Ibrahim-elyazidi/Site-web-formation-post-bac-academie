{% extends 'base.html.twig' %}

{% block title %}Referent{% endblock %}

{% block body %}
    <h1>Referent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ referent.id }}</td>
            </tr>
            <tr>
                <th>NomReferent</th>
                <td>{{ referent.nomReferent }}</td>
            </tr>
            <tr>
                <th>PrenomReferent</th>
                <td>{{ referent.prenomReferent }}</td>
            </tr>
            <tr>
                <th>MailReferent</th>
                <td>{{ referent.mailReferent }}</td>
            </tr>
            <tr>
                <th>TelephoneReferent</th>
                <td>{{ referent.telephoneReferent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_referent_index') }}">back to list</a>

    <a href="{{ path('app_referent_edit', {'id': referent.id}) }}">edit</a>

    {{ include('referent/_delete_form.html.twig') }}
{% endblock %}
