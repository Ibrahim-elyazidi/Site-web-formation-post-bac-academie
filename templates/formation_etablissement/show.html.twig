{% extends 'base.html.twig' %}

{% block title %}FormationEtablissement{% endblock %}

{% block body %}
    <h1>FormationEtablissement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation_etablissement.id }}</td>
            </tr>
            <tr>
                <th>SiteWeb</th>
                <td>{{ formation_etablissement.siteWeb }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_formation_etablissement_index') }}">back to list</a>

    <a href="{{ path('app_formation_etablissement_edit', {'id': formation_etablissement.id}) }}">edit</a>

    {{ include('formation_etablissement/_delete_form.html.twig') }}
{% endblock %}
