<form method="post" action="{{ path('app_formation_etablissement_delete', {'id': formation_etablissement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation_etablissement.id) }}">
    <button class="btn">Delete</button>
</form>
